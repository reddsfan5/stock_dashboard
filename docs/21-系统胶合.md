# 21 · 系统胶合（标的上下文与每日节奏）

Phase 4 把选股、观察池、训练、日记串成「按代码看一页」和「每日清单」，不新增微服务，仍共用 `127.0.0.1:8765`。

## 标的上下文

- 页面：`http://127.0.0.1:8765/symbol.html?code=sh600519`
- API：`GET /api/symbol/context?code=sh600519`
- 假设：`POST /api/symbol/hypothesis`、`POST /api/symbol/hypothesis/status`

一页汇总：

1. 最新选股命中（解析 `output/dashboard.html` 的 `TAB_CODES`）
2. 观察池状态与次日跟踪
3. 近期训练会话与决策数
4. 选股日记案例链接
5. 轻量假设生命周期：`hypothesis → backtested → training → noted`
6. 可选选股→交易报告中的该代码样本

数据复用：`state/watchlist.sqlite3`、`state/training_sessions.sqlite3`、`state/stock_journal.sqlite3`，新建 `state/hypotheses.sqlite3`。

## 每日操盘清单

```bash
python -m scripts.update_cache
python -m scripts.update_cache --only watchlist_track   # 可选单独刷新跟踪
python -m scripts.reports.gen_daily_ops                 # 清单 + 板块强度快照
```

- 页面：`output/daily_ops.html`（工作台「日常看盘」首项；8765 下也可打开）
- 快照：`cache/sector_strength_snapshot.json`
- 状态：`cache/daily_update_status.json`（`update_cache` 已写入）

## 入口链接

工作台、观察池、仪表盘 K 线侧栏、T+1 训练、选股日记均已链到标的上下文页。
