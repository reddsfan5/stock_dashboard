# 系统 python（/usr/local/bin/python）的 demo 依赖
# 项目基础依赖见根目录 requirements.txt（akshare/pandas/numpy/pyarrow/tqdm/pyyaml/baostock）
quantstats>=0.0.62
IPython            # quantstats 0.0.7x 的 reports 硬依赖（未声明）
TA-Lib>=0.5.0      # 需先 brew install ta-lib
pyecharts>=2.0
efinance>=0.5.9
